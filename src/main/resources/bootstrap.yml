---
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Swarna8282/properties
          skipSslValidation: true
  application:
    name: my-config-server

server:
  port: 9999

