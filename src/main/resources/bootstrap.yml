---
spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.prod.fedex.com/3745693/config-repo.git
          clone-on-start: true
  application:
    name: my-config-server

server:
  port: 9999